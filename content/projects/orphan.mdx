A Brief file with no matching `projects.ts` entry - used to prove
`generateStaticParams` enumerates from the validated `projects.ts` set and
never globs `content/projects/`, so this file is never published as a route
(FR-9, enumerate-not-glob).
