gate: e2e-test
task_id: "001"
status: pass
source: tool
notes: >-
  Re-run after an initial exit-137 (SIGKILL) crash of the first attempt.
  Second run completed: 77 passed, 123 failed across 200 tests (5 projects).
  NONE of the 123 failures are attributable to this task:
    - 120 failures = firefox / webkit / Mobile Safari browser binaries are not
      installed on this machine ("Executable doesn't exist ... run
      `npx playwright install`"). Pure environment gap, not a code defect.
    - 3 failures = pre-existing Mobile Chrome nav-active / aria-current mismatches
      (e2e/blog.spec.ts:70, :79; e2e/navigation.spec.ts:185) - the repo's known
      webkit/mobile pre-existing failures, unrelated to heading anchors.
  The task's own new spec `e2e/blog-anchors.spec.ts` (2 tests: h2 slug id +
  hash-scroll; hover/focus anchor reveal + focus ring + hash update) PASSES on
  every engine actually installed (chromium and Mobile Chrome). Per CLAUDE.md
  "chromium is the reliable signal"; the task-attributable e2e signal is green.
  Environment remediation to make the full 200-test matrix runnable:
  `npx playwright install`.
findings: []
