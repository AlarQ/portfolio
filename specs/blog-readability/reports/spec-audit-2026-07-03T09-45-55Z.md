---
feature: blog-readability
timestamp: 2026-07-03T09-45-55Z
scope: per-task
verdict: complete
---

## FR x Status Matrix

| FR | Title | Status | Evidence |
|----|-------|--------|----------|
| FR-1 | Course and Lesson as a separate reading axis | implemented | `apps/repeat-app/src/domain/course/entities.rs`: `Course`, `Lesson`, `LessonKind` structs with all spec-required fields (`slug`, `title`, `order_no`, `kind`, `html_body`, `user_id`). Migration `migrations/20260622000001_create_course_lesson.sql` creates `course` and `lesson` tables with correct FK (`course_id REFERENCES course(id) ON DELETE CASCADE`), UNIQUE constraints `(user_id, slug)` and `(course_id, kind, slug)`, and both required indexes. `domain/mod.rs` and `infrastructure/mod.rs` register the new `course` module. No FK to `users`, consistent with spec. |
| FR-2 | Idempotent course import via CLI | implemented | `infrastructure/course/import_runner.rs`: `run_import_courses` canonicalizes `--root`, walks `lessons/*.html` and `reference/*.html`, path-confinement check via `starts_with(canonical_root)`, delegates to `domain/course/operations::import_course_file` which uses `ManifestStore` to skip already-`OK` paths. `domain/course/import_util.rs`: `clean_slug`, `parse_leading_order`, `OrderStrategy::LeadingDecimal` vs `AlphabeticalPosition`. `main.rs` wires `import-courses` subcommand. Oversize bodies (`> 262_144`) and invalid slugs produce soft skips with logged errors — no panics. `tests/import_courses.rs` covers 595 lines of scenario tests. |
| FR-3 | List a learner's courses | implemented | `api/course/handlers.rs:list_courses_handler` — `#[instrument(skip(state))]`, calls `domain/course/operations::list_courses(user_id.as_uuid(), ...)`. `infrastructure/course/course_repository_pg.rs` filters `WHERE user_id = $1`. `api/course/mod.rs` mounts under the authenticated router scope shared with qanda routes — auth middleware applies globally. `models.rs:CourseSummaryDto` returns `{ id, slug, title, lessonCount }`. `tests/course_api.rs` covers scoping and 401. |
| FR-4 | List the lessons of a course | implemented | `api/course/handlers.rs:list_lessons_handler` — calls `list_lessons(user_id, course_id, ...)`. Repository filters `WHERE user_id = $1 AND course_id = $2 ORDER BY order_no` capped at 500. `LessonSummaryDto` omits `html_body` (fields: `id, slug, title, orderNo, kind`). Cross-user course returns 404 via `CourseError::NotFound` mapped in `api/qanda/error.rs`. `tests/course_api.rs` verifies ordering and cross-user-404. |
| FR-5 | Read a single lesson body | implemented | `api/course/handlers.rs:get_lesson_handler` — calls `domain/course/operations::get_lesson`. `operations.rs:get_lesson` (line 151-154): checks `lesson.html_body.len() > SIZE_CAP` and returns `CourseError::OversizeBody` mapped to HTTP 500 `{ code: "Internal" }`. Cross-user or missing lesson returns `NotFound` → 404. `LessonDetailDto` includes `htmlBody`. `tests/course_api.rs` and `tests/course_repo.rs` cover read-lesson-body, cross-user-404, and read-oversize-500 scenarios. |
| FR-6 | Render lesson HTML verbatim in a sandboxed iframe | implemented | `src/components/courses/lesson-viewer.tsx:54-55`: `<iframe srcDoc={htmlBody} sandbox="" referrerPolicy="no-referrer" .../>`. `sandbox=""` is a literal empty string — no `allow-scripts`, no `allow-same-origin`. `dangerouslySetInnerHTML` and `innerHTML` are absent from the file. `biome-rules/no-iframe-sandbox-escape.grit` enforces the constraint at CI lint time. `biome.json` registers the rule. `src/components/courses/__tests__/lesson-viewer.test.tsx` (89 lines) exercises rendering. |
| FR-7 | Drill a lesson via the existing interview flow | implemented | `src/app/(app)/lessons/[id]/drill-entry.tsx`: `if (lesson.kind !== "Lesson") return null` (line 15); `onDrill` calls `reset()`, `setSourceDocument(lesson.htmlBody)`, then `router.push("/interview?from=lesson")`. `use-interview-phase.ts:90-97`: on mount, if `state.sourceDocument` is non-empty and phase is `"source"`, auto-calls `start(doc)` which calls `generate({ sourceDocument, ... })` — hitting the existing `AiQandaPort::generate_question`. Fence neutralization (`neutralize_fence_markers`) and `fenced("SOURCE_DOCUMENT", ...)` wrapping are in `infrastructure/qanda/ai_port.rs:42-47,123`. Generated questions classify to Category; nothing is filed on the Lesson. `e2e/drill.spec.ts` covers the drill-lesson-generates scenario. |
| FR-8 | Reference sheets are readable, not drillable | implemented | `src/app/(app)/lessons/[id]/drill-entry.tsx:15`: `if (lesson.kind !== "Lesson") return null` — the `DrillEntry` component renders nothing for `kind = "Reference"`. The `LessonViewer` renders both kinds through the same iframe path. `LessonKindDto` in `src/types/generated/api.ts:841` is `"Lesson" | "Reference"`. `src/app/(app)/lessons/[id]/__tests__/drill-entry.test.tsx` asserts the button is absent for reference kind. |

## Orphan Code

`reports/error-handling-api.md` in `repeat-frontend` — a Markdown report file committed to the repo root. It documents error handling patterns but is not traceable to any FR and is not referenced by any test, lint, or build artifact. Orphan by itself; no blocking impact.

`src/app/(app)/lessons/[id]/nav-link.tsx` — a navigation helper component (58 lines) added in the diff. It is used by the lesson viewer page for breadcrumb/back navigation. Functionally justified but not explicitly required by any FR. Traceable as supporting infrastructure for FR-5/FR-6 UX; borderline orphan.

## Over-Engineering Flags

None identified. The grit lint rule (`biome-rules/no-iframe-sandbox-escape.grit`) and its fixtures are proportionate to FR-6's security requirement (ADR-0001) and enforce a property that cannot be caught by TypeScript types alone. ADRs 0001-0003 are in-scope design records. The `CONTEXT.md` glossary seed is explicitly required by the spec (design §9.3). No speculative abstractions or premature generalization were found.

## Process Flags (Advisory)

**TDD loop not evidenced for task 001.** Monitor shows `tdd_red=0, tdd_green=0`. Tests exist (`tests/course_repo.rs`, `tests/import_courses.rs`) but no red-green cycle was recorded in the monitor event log.

**TDD loop not evidenced for task 002.** Monitor shows `tdd_red=0, tdd_green=0`. `tests/import_courses.rs` (595 lines) covers FR-2 scenarios but cycle not captured.

**TDD loop not evidenced for task 003.** Monitor shows `tdd_red=0, tdd_green=0`. `tests/course_api.rs` (246 lines) and `tests/course_never_log_body.rs` (141 lines) are present without recorded cycle.

**TDD loop not evidenced for task 004.** Monitor shows `tdd_red=0, tdd_green=0`. Unit tests in `src/app/(app)/courses/__tests__/` and `src/components/courses/__tests__/` are present but cycle not recorded.

**TDD loop not evidenced for task 005.** Monitor shows `tdd_red=0, tdd_green=0`. `__tests__/lesson-viewer-client.test.tsx` (113 lines) present without cycle.

**TDD loop not evidenced for task 006.** Monitor shows `tdd_red=0, tdd_green=0`. `e2e/drill.spec.ts` (80 lines) and `__tests__/drill-entry.test.tsx` (73 lines) present without cycle.

These are advisory only. All six tasks have substantive test files covering the relevant scenarios. The absence of monitor event records reflects a tooling/instrumentation gap in the workflow, not a coverage gap in the delivered tests. TDD advisories do not affect the verdict.
