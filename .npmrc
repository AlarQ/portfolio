# pnpm config. Default isolated node_modules (strict — surfaces phantom deps).
# Add scoped public-hoist-pattern[] entries here only if a tool breaks on a
# missing hoisted dep; avoid shamefully-hoist unless nothing else works.
